# 海潮点阵材质固定候选

导入 [item.t3e.txt](item.t3e.txt)，点击“材质试验”。显示版本0.4.0-test、objectVersion 4、引擎0.4.1，沿用原rootId和存档。

Imagegen生成六面参考；游戏使用根据参考重新绘制的24×24点阵，12色共享调色板、RLE4硬编码。六面数据1941字节，完整Lua数据文件2423字节。PNG和SVG仅为制作预览，不会在运行时加载。

三个主体包括六面符文匣、拉伸方碑和三角面晶石。可选图、旋转UV、转动主体/父节点、解除绑定。测试页不改变魔方局面与计时。

本地测试共112项通过，覆盖原有引擎/游戏/包系统及新解码、无重叠色块合并、UV旋转、三角裁剪、变换跟随、预算失败保留原材质、原始几何拾取和退出恢复。之后仅重跑受最终修改影响的5项像素材质测试和17项魔方测试，均通过。完整TRP3包在上游Lua脚本沙盒中启动后，也实际打开了材质场景。

浏览器实际点击了UV旋转和父节点旋转，点阵保持附着；默认三主体2598显示面、1080次绘制。WoW客户端的像素显示和帧耗时仍待实机确认，`inGameVerified` 保持false。

- sourceHash：`0374bbd7102cddb50a804e56824fc59ebdbd55ebcaf87b4719feb336f4fc67e2`
- exportHash：`64af02586889523fe5c68f0a88cbed1fbe7b1a05d3d1ac4e02a062829539c86d`
- 导入字符串90713字符，宏252字节；development构建保留源码。

参考图、PNG/SVG预览、生成提示词和压缩方式见[美术说明](../../art/README.md)。此固定副本独立于后续dist重建。
