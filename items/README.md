# Future TRP3 Items

Create one subdirectory per independently deployable prop, for example `items/scene-recorder/`. Keep its Lua script and a short installation note together so it can be copied into Total RP 3 without relying on the repository layout at runtime.
