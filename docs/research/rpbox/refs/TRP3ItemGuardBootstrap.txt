/run RPBG0=TRP3_API.script.runLuaScriptEffect;TRP3_API.script.runLuaScriptEffect=function(c,a,s)TRP3_API.script.runLuaScriptEffect=RPBG0;a=a or{};a._G=_G;return RPBG0(c,a,s)end

This is a one-shot bootstrap, not a permanent blanket _G injection.

1. Run this macro.
2. Immediately use the trusted item containing TRP3ItemGuard.lua.
3. The wrapper restores TRP3's original runLuaScriptEffect before the guard runs.
4. TRP3ItemGuard.lua then installs its own scanner and gated injection wrapper.

If any other Lua Script Effect runs between steps 1 and 2, that effect receives
the one-shot _G reference instead. Run the bootstrap again and immediately use
the trusted guard item.
